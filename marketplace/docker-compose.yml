services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: pinwall
      POSTGRES_PASSWORD: pinwall
      POSTGRES_DB: pinwall_marketplace
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
